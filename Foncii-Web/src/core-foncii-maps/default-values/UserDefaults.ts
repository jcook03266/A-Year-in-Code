// Dependencies
// Types
import { CoordinatePoint } from "../../__generated__/graphql";

// Default Properties to apply for user interface components with optional parameters
// Map Box
export const defaultMapBoxCenter: CoordinatePoint = {
  lat: 40.7428,
  lng: -74.016,
};

export const defaultMapBoxZoomLevel: number = 9;
